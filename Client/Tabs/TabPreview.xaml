<UserControl x:Class="Proryv.AskueARM2.Client.Visual.TabPreview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	 xmlns:arm="clr-namespace:Proryv.AskueARM2.Client.Visual"    
    Height="210" Width="210"  BorderThickness="0,0,0,0" BorderBrush="{x:Null}" Background="{x:Null}" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Name="userControl">
    <Grid x:Name="grid" Background="{x:Null}">
        <Border Margin="0,0,0,0" BorderThickness="2"  Background="{StaticResource BaseLightBackground}" BorderBrush="{StaticResource BaseLightBorder}">
        </Border>
        <Image x:Name="content" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Margin="8,10,8,5" Opacity="100"/>
    </Grid>
</UserControl>