<sap:WorkflowElementDialog x:Class="Proryv.Workflow.Activity.ARM.DialogSetTechQualityLastValues"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:sap="clr-namespace:System.Activities.Presentation;assembly=System.Activities.Presentation" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Width="300" Height="400" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
    <Grid>
        <ListBox Name = "ListQuality" Margin="0,0,2,0" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
            <ListBox.ItemTemplate>
            <DataTemplate>
                <CheckBox Name="CheckBoxZone" Content="{Binding ParamName}" IsChecked="{Binding IsChecked}" Margin="0,5,0,0"/>
            </DataTemplate>
            </ListBox.ItemTemplate>
       </ListBox>
        
    </Grid>
</sap:WorkflowElementDialog>
